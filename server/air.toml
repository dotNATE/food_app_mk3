# .air.toml
root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/main ."
bin = "tmp/main"
full_bin = "false"
include_ext = ["go"]
exclude_dir = ["vendor", "tmp"]
exclude_file = []
delay = 1000
